const utils = require('./core/utils');
const config = require('./core/config');
const server = require('./core/server');
const DB = require('./DB');

module.exports = {
    utils,
    config,
    server,
    DB
};
