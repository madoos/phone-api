const utils = require('./lib/utils');
const config = require('./lib/config');
const server = require('./lib/server');
const DB = require('./DB');

module.exports = {
    utils,
    config,
    server,
    DB
};
